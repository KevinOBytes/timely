# Copilot Instructions

This project follows the operating rules in `AGENTS.md`.

1. Read and apply `../AGENTS.md` before making changes.
2. Keep `FEATURES.md` synchronized with `REQUIREMENTS.md`.
3. Update `README.md` and `/docs` for behavior or architecture changes.
4. Prefer TypeScript and verified security defaults.
